import { OrderDetailsTable } from 'entities/Order/ui/CreateOrderTable/CreateOrderTable';
import { type ReturnComponentType } from 'shared/types/ReturnComponentType';

export const OrderRegistration = (): ReturnComponentType => {
  return <OrderDetailsTable />;
};
