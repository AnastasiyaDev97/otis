import { type ReturnComponentType } from 'shared/types/ReturnComponentType';
import { AppLayout } from 'widgets';
import './index.scss';

export const App = (): ReturnComponentType => {
  return <AppLayout>hkhkjh</AppLayout>;
};
